@model TabloidMVC.Models.ViewModels.PostComments

@{
    ViewData["Title"] = "Index";
}

<h1>Comments</h1>

<p>
    <a class="btn btn-primary" asp-action="Create" asp-route-id="@Model.Post.Id">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PostId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserProfileId)
            </th>*@
            <th>
               Subject
            </th>
            <th>
               Content
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.CreateDateTime)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Comment) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserProfileId)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Subject)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Content)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.CreateDateTime)
            </td>*@
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id}) |
                @*@Html.ActionLink("Details", "Details", new { id=item.Primar }) |*@
                @Html.ActionLink("Delete", "Delete", new { id=item.Id})
            </td>
        </tr>
}
    </tbody>
</table>

<a asp-controller="Post" asp-action="Details" asp-route-id="@Model.Post.Id" class="btn btn-outline-primary mx-1" title="Tag Manager">
    Back
</a>
